OPENAI_API_KEY=
# Optional — experimental full-video visual timeline (Pass 1). If unset or invalid, OpenAI is used.
GEMINI_API_KEY=
# openai | gemini (default: openai when unset)
VISION_PROVIDER=openai
GEMINI_MAX_VIDEO_DURATION_SECONDS=600
# Optional override for Gemini vision model (default: gemini-2.0-flash)
# GEMINI_VISION_MODEL=gemini-2.0-flash
# elevenlabs | openai (default: openai — uses OpenAI TTS while ElevenLabs credits are unavailable)
TTS_PROVIDER=openai
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_JORDAN=obcEwiLH5xERuD4ZBXW4
ELEVENLABS_VOICE_GEORGE=
ELEVENLABS_VOICE_DANIEL=
ELEVENLABS_VOICE_BRIAN=
ELEVENLABS_VOICE_ALICE=
ELEVENLABS_VOICE_MATILDA=
ELEVENLABS_VOICE_SARAH=
SUPABASE_URL=https://wvnumokefxbfiysfqaed.supabase.co
SUPABASE_SECRET_KEY=
# Optional: Postgres URI from Supabase Dashboard → Settings → Database (for npm run migrate:three-roll only).
# DATABASE_URL=
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_URL=
PORT=3001
WATERMARK_TEXT=RollAI
# FFmpeg: max narration clips mixed in one graph (default 8, max 32). Lower = stabler; raises job success vs full commentary coverage.
ROLLAI_MAX_AMIX_SEGMENTS=8
# Set to 1 to stream FFmpeg stderr lines to the worker log as they arrive (verbose).
ROLLAI_FFMPEG_VERBOSE=0
